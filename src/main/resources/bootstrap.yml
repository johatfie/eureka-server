spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8071
      fail-fast: true
      max-attempts: 20
      max-interval: 15000
      initial-interval: 10000
    loadbalancer:
      ribbon:
        enabled: false